<%= form_for(@journal_submission, :html => { :class => "form-horizontal", :role => "form" }) do |f| %>
<%#= form_for(@journal_submission, :journal_id => @journal_journal.id, :html => { :class => "form-horizontal", :role => "form" }) do |f| %>
<%#= form_for([@journal_journal, @journal_submission], :html => { :class => "form-horizontal", :role => "form" }) do |f| %>
  <% if @journal_submission.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@journal_submission.errors.count, "error") %> prohibited this journal_submission from being saved:</h4>

      <ul>
      <% @journal_submission.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

      <%= f.hidden_field :journal_id, :class => "form-control" %>
  <div class="form-group">
    <%= f.label :title, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :title, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :abstract, :class => "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_area :abstract, :class => "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit :class => "btn btn-primary" %>
    </div>
  </div>
<% end %>
